<div>
<form method="POST" action="{{#if _id}}/drones/{{_id}}/edit{{else}}/drones/create{{/if}}">
  <div class="mb-3">
    <label for="name" class="form-label">Name</label>
    <input type="text" name="name" class="form-control" id="name" value="{{name}}">
  </div>
  <div class="mb-3">
    <label for="propellers" class="form-label">Propellers</label>
    <input type="text" name="propellers" class="form-control" id="propellers" value="{{propellers}}">
  </div>
  <div class="mb-3">
    <label for="maxSpeed" class="form-label">Max Speed</label>
    <input type="text" name="maxSpeed" class="form-control" id="maxSpeed" value="{{maxSpeed}}">
  </div>
  <button type="submit" class="btn btn-primary">{{#if _id}}Update{{else}}Create{{/if}}</button>
</form>   
</div>