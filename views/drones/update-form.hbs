<h2>Update the drone {{drone.name}}</h2>

<form action="/drones/{{drone.id}}/edit" method="POST">
    <label>Name:
        <input type="text" name="name" value="{{drone.name}}" />
    </label>

    <label>Propellors:
        <input type="number" name="propellors" value="{{drone.propellors}}" />
    </label>

    <label>Max Speed:
        <input type="number" name="maxSpeed" value="{{drone.maxSpeed}}" />
    </label>

    <button>Save changes</button>
</form>