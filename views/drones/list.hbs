<h2 class="my-3">Available drones</h2>
<div class="container">
  <div class="row align-items-start">

{{#each drones}}
<div class="card col-3 me-5 border-0">
    <img src="/images/drone.jpeg" class="card-img-top" alt="default-drone" />
    <h3  class="my-3">{{name}}</h3>
    <p>Propellers: {{propellers}}</p>
    <p>Max. Speed: {{maxSpeed}}</p>

   <div>  <a href='/drones/{{_id}}/edit'class="btn btn-outline-primary my-3"> Edit Drone</a> </div>
    <form action="/drones/{{_id}}/delete" method="post" >
<button type="submit" class="btn btn-outline-danger my-3">Delete the drone</button>
</form>

</div>
{{/each}}
 </div>
</div>