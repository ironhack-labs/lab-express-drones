<h2>Available drones</h2>
<a href='/drones/create'>Create a Drone</a>
{{#each this.dronesDB}}
    <p>Drone Name: {{this.name}}</p>
    <p>No. of Propellers: {{this.propellers}}</p>
    <p>Max Spped Limit: {{this.maxSpeed}}</p>
    <a href='/drones/edit/{{this._id}}'>Edit</a>
    <form action="/drones/delete/{{this._id}}" method="POST">
        <button type="submit">Delete drone</button>
    </form>
{{/each }}