<h2>Available drones</h2>
{{!-- <div>
     {{#each drones as |drone|}}
    <ul>
       
        <li>
            <h3>Name: {{drone.name}}</h3>
                    
        </li>
        <li>
            <h5>Propellers: {{drone.propellers}}</h5>
        </li>
        <li>
            <h5>Max Speed: {{drone.maxSpeed}}</h5>
        </li>
        <li>
            <a href="/drones/{{drone.id}}/edit">Update</a>
        </li>
        <li>
            <form action="/drones/{{drone.id}}/delete" method="POST">
                <button type="submit">Delete Drone</button>
            </form>
            
        </li>
       
    </ul>
    </div> --}}
     {{!-- {{/each}} --}}
     <div class="container">
         <div class="row">
             {{#each drones as |drone|}}
             <div class="col-3">
                   
                        <div class="card" style="width: 18rem;">
                                <div class="card-body">
                                    <h5 class="card-title">Drone :{{drone.name}} </h5>
                                    <p class="card-text">Propellers: {{drone.propellers}}</p>
                                    <p class="card-text">Max Speed: {{drone.maxSpeed}}</p>
                                    
                                    <a href="/drones/{{drone.id}}/edit" class="card-link btn btn-primary">Update</a>
                                    <form action="/drones/{{drone.id}}/delete" method="POST">
                                        <button type="submit" class="btn btn-danger">Delete Drone</button>
                                    </form>
                                </div>
                        </div>
                   
             </div>
              {{/each}}
         </div>
     </div>
   
     

