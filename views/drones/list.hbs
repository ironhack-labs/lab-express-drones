<h2>Available drones</h2>

{{#each drones}}
  <div>
    <h3>{{title}}</h3>
    <p>Propellers: {{propellers}}</p>
    <p>Maximum Speed: {{maxSpeed}}</p>
    <a href="/drones/{{_id}}">[ Details ]</a>
    <a href="/drones/{{_id}}/edit">[ Edit ]</a>
    <p>
    <form action="/drones/{{_id}}/delete" method="POST">
      <button>Delete</button>
    </form>
    </p>
  </div>
{{/each}}
