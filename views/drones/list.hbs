<h2>Available drones</h2>

    {{#each drone}}
        <ul>
            <li>Name: {{name}}</li>
            <li>Propellers: {{propellers}}</li>
            <li>Max Speed: {{maxSpeed}}</li>
        </ul>
    {{/each}}