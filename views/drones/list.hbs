<h2>Available drones</h2>
<div class="drone-wrapper">
    {{#each allDrones}}
    <h3>{{this.name}}</h3>
    <p>Specs</p>
    <ul>
        <li>Propellors: {{this.propellors}}</li>
        <li>Speed: {{speed}} KM/H</li>
    </ul>
    <a href="/drones/{{this.id}}/edit" class="edit-drone">Edit</a>
    <form action="/drones/{{this.id}}/delete" method="post">
    <button type='submit'>Delete</button>
    </form>
    {{/each}}
</div>
<div class="add-button">
<a href="/drones/create">Add a new drone</a>
</div>